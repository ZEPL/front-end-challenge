import Room from 'app/actions/room';
import Session from 'app/actions/session';
import Message from 'app/actions/message';

const Actions = {
  Room,
  Session,
  Message,
};

export default Actions;
