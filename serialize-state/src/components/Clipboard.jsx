import React from 'react';

const Clipboard = () => (
  <section>
    <input placeholder="Paste state here" />
    <button>Save State</button>
    <button>Copy State</button>
  </section>
);

export default Clipboard;
