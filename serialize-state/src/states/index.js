import { MARVEL, DC, SPIDEY, HULK, BATMAN, SUPERMAN } from '../constants';

// default state
const initialState = {
  userName: '',
  comic: undefined,
  superHeroes: []
};

export default initialState;
